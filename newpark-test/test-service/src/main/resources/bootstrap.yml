server:
  port: 60001
# Mybatis plus 配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  type-aliases-package: com.xxs.market.entity.*
  mapper-locations: classpath:mapping/*/*.xml
